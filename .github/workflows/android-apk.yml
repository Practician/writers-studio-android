name: Android APK

on:
  push:
    branches: [master, main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-apk:
    name: Проверка и APK
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Получить исходный код
        uses: actions/checkout@v4

      - name: Настроить Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Настроить Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: gradle

      - name: Установить зависимости
        run: npm ci

      - name: Проверить TypeScript
        run: npm run lint

      - name: Запустить тесты
        run: npm test

      - name: Собрать web bundle внутри APK
        run: npm run build

      - name: Синхронизировать Capacitor Android
        run: npx cap sync android

      - name: Подготовить release signing
        id: signing
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
          RELEASE_STORE_PASSWORD: ${{ secrets.RELEASE_STORE_PASSWORD }}
          RELEASE_KEY_ALIAS: ${{ secrets.RELEASE_KEY_ALIAS }}
        run: |
          if [ -n "$KEYSTORE_BASE64" ]; then
            echo "$KEYSTORE_BASE64" | base64 --decode > android/release-key.jks
            chmod 600 android/release-key.jks
            echo "store_file=${{ github.workspace }}/android/release-key.jks" >> "$GITHUB_OUTPUT"
            echo "store_password=${RELEASE_STORE_PASSWORD:-BookStore-Release-2026-Secure}" >> "$GITHUB_OUTPUT"
            echo "key_alias=${RELEASE_KEY_ALIAS:-bookstore-key}" >> "$GITHUB_OUTPUT"
          elif [ -f "android/keystore/release-key.jks" ]; then
            echo "store_file=${{ github.workspace }}/android/keystore/release-key.jks" >> "$GITHUB_OUTPUT"
            echo "store_password=${RELEASE_STORE_PASSWORD:-BookStore-Release-2026-Secure}" >> "$GITHUB_OUTPUT"
            echo "key_alias=${RELEASE_KEY_ALIAS:-bookstore-key}" >> "$GITHUB_OUTPUT"
          else
            echo "store_file=" >> "$GITHUB_OUTPUT"
          fi

      - name: Собрать APK
        working-directory: android
        env:
          APP_VERSION_CODE: ${{ github.run_number }}
          RELEASE_STORE_FILE: ${{ steps.signing.outputs.store_file }}
          RELEASE_STORE_PASSWORD: ${{ steps.signing.outputs.store_password }}
          RELEASE_KEY_ALIAS: ${{ steps.signing.outputs.key_alias }}
          RELEASE_KEY_PASSWORD: ${{ steps.signing.outputs.store_password }}
        run: |
          chmod +x gradlew
          if [ -n "${{ steps.signing.outputs.store_file }}" ]; then
            ./gradlew :app:assembleRelease --no-daemon
          else
            ./gradlew :app:assembleDebug --no-daemon
          fi

      - name: Подготовить APK для скачивания
        id: apk
        run: |
          VERSION_NAME=$(grep -E '^[[:space:]]*versionName ' android/app/build.gradle | sed -E 's/.*"([^"]+)".*/\1/')
          APK_NAME="WritersStudio-Android-v${VERSION_NAME}-build-${{ github.run_number }}.apk"
          if [ -f "android/app/build/outputs/apk/release/app-release.apk" ]; then
            cp android/app/build/outputs/apk/release/app-release.apk "$APK_NAME"
            KIND="подписанная release-сборка (постоянный ключ)"
          else
            cp android/app/build/outputs/apk/debug/app-debug.apk "$APK_NAME"
            KIND="debug-сборка"
          fi
          echo "name=$APK_NAME" >> "$GITHUB_OUTPUT"
          echo "version=$VERSION_NAME" >> "$GITHUB_OUTPUT"
          echo "kind=$KIND" >> "$GITHUB_OUTPUT"

      - name: Сохранить APK как workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: writers-studio-android-${{ github.sha }}
          path: ${{ steps.apk.outputs.name }}
          if-no-files-found: error
          retention-days: 14

      - name: Опубликовать APK в GitHub Releases
        if: github.event_name == 'push'
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: v${{ steps.apk.outputs.version }}-build-${{ github.run_number }}
          RELEASE_TITLE: Writers Studio Android v${{ steps.apk.outputs.version }} · сборка ${{ github.run_number }}
          APK_NAME: ${{ steps.apk.outputs.name }}
          APK_KIND: ${{ steps.apk.outputs.kind }}
        run: |
          gh release create "$RELEASE_TAG" \
            "$APK_NAME" \
            --repo "$GITHUB_REPOSITORY" \
            --target "$GITHUB_SHA" \
            --title "$RELEASE_TITLE" \
            --notes "$APK_KIND. Установочный APK для коммита $GITHUB_SHA. Writers Studio работает автономно; ключ ИИ добавляется пользователем после установки." \
            --latest

# Для постоянных обновлений добавьте в repository secrets:
# KEYSTORE_BASE64, RELEASE_STORE_PASSWORD, RELEASE_KEY_ALIAS.
# Keystore и пароли не хранятся в Git, GitHub Releases или APK.
